{
  "name": "johel",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "db:migrate": "pnpm --filter api prisma migrate dev"
  },
  "engines": {
    "node": ">=20"
  }
}

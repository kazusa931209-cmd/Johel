/** Configurable whole-line DeJob chrome patterns (case-insensitive). */
export const dejobLinePatterns: RegExp[] = [
  /^view more jobs of .+\s*>\s*$/i,
  /^dejob\s+blazes\s+new\s+trials?\s+for\s+web\s*3\.?0?\s*$/i,
  /^dejob\s*$/i,
  /^about us\s*$/i,
  /^join us\s*$/i,
  /^for talents\s*$/i,
  /^post resume\s*$/i,
  /^find job\s*$/i,
  /^find web3 companies\s*$/i,
  /^for companies\s*$/i,
];
